const dragOverDefaults = {
    dragover: ($event) => {
        $event.preventDefault();
    },
    dragenter: ($event) => {
        $event.preventDefault();
    },
    dragleave: ($event) => {
        $event.preventDefault();
    },
};

export default dragOverDefaults